apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo-get-task-api-svc
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-todo-get-task-api
  ports:
    - port: {{ .Values.gettask.ServicePort }}
      targetPort: {{ .Values.gettask.port }}
